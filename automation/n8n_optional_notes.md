# Optional n8n Notes
n8n can orchestrate local scripts, but it is not required. If enabled with Podman, bind it to localhost, avoid cloud credentials by default, and keep work/personal workflows separate. Use explicit script arguments such as `--scope work` or `--scope personal`.
